#pragma once

#include <string>

#include <glm/glm.hpp>
#include <glm/gtc/matrix_transform.hpp>

struct ComponentA {
	std::string text;
};

struct ComponentB {
	std::string text;
};

struct ComponentC {
	std::string text;
};

struct ComponentD {
	std::string text;
};
